# ********RoostGPT********

# Test generated by RoostGPT for test raml-testing using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /_/tenant_post for http method type POST in artillery framework
# RoostTestHash=4b8c14d754
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "__tenant_post_others.csv"
    fields:
      - "module_from"
      - "module_to"
      - "purge"
      - "parameters"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Tenant API non-success scenario"
    flow:
      - log: "Sending POST request with invalid module_to value"
      - post:
          url: "/_/tenant"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            module_from: "{{ module_from }}"
            module_to: "invalid_module"
            purge: "{{ purge }}"
            parameters: "{{ parameters }}"
          expect:
            - statusCode: 400
            - hasProperty: "errors"
            - hasProperty: "errors[0].message"
            - equals: ["errors[0].message", "Invalid module_to value"]
      - think: 1

      - log: "Sending POST request with missing module_from value"
      - post:
          url: "/_/tenant"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            module_to: "{{ module_to }}"
            purge: "{{ purge }}"
            parameters: "{{ parameters }}"
          expect:
            - statusCode: 400
            - hasProperty: "errors"
            - hasProperty: "errors[0].message"
            - equals: ["errors[0].message", "Missing module_from value"]
      - think: 1

      - log: "Sending POST request to invalid URL"
      - post:
          url: "/_/invalid_url"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            module_from: "{{ module_from }}"
            module_to: "{{ module_to }}"
            purge: "{{ purge }}"
            parameters: "{{ parameters }}"
          expect:
            - statusCode: 404
      - think: 1
