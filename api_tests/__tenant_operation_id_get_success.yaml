# ********RoostGPT********

# Test generated by RoostGPT for test raml-testing using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /_/tenant/{operation_id}_get for http method type GET in artillery framework
# RoostTestHash=139c0a46b1
# 
# 

# ********RoostGPT********
config:
  target: "${processEnvironment.API_HOST}"
  plugins:
    expect: {}
  payload:
    path: "__tenant_operation_id_get_success.csv"
    fields:
      - operation_id
    skipHeader: true
    order: sequence
    delimiter: "^|^"
  environments:
    production:
      target: "${processEnvironment.API_HOST}"
      headers:
        Authorization: "Bearer ${processEnvironment.AUTH_TOKEN}"
      
scenarios:
  - name: "Test Scenario: Successful API Requests"
    flow:
      - log: "API Request: Get tenant job"
      - get:
          url: "/_/tenant/{{ operation_id }}?wait=0"
          headers:
            Authorization: "Bearer ${processEnvironment.AUTH_TOKEN}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "tenant"
            - hasProperty: "tenantAttributes"
            - hasProperty: "complete"
            - hasProperty: "error"
            - hasProperty: "messages"
      - think: 1
