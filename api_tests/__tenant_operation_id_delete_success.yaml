# ********RoostGPT********

# Test generated by RoostGPT for test raml-testing using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /_/tenant/{operation_id}_delete for http method type DELETE in artillery framework
# RoostTestHash=406671589b
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "__tenant_operation_id_delete_success.csv"
    fields:
      - "operation_id"
    skipHeader: true
    delimiter: "^|^"
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
      Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Delete job scenario"
    flow:
      - log: "Initiating delete job request"
      - delete:
          url: "/_/tenant/{{ operation_id }}"
          expect:
            - statusCode: 204
      - think: 1
