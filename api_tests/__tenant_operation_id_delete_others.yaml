# ********RoostGPT********

# Test generated by RoostGPT for test raml-testing using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /_/tenant/{operation_id}_delete for http method type DELETE in artillery framework
# RoostTestHash=406671589b
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "__tenant_operation_id_delete_others.csv"
    fields: ["operation_id"]
    skipHeader: true
    delimiter: "^|^"
  defaults:
    headers:
      SECURITY_SCHEMA_NAME: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Test non-success API responses"
    flow:
      - log: "Sending a DELETE request to an invalid URL"
      - delete:
          url: "/invalid/url/{{ operation_id }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending a DELETE request without auth token"
      - delete:
          url: "/_/tenant/{{ operation_id }}"
          headers:
            SECURITY_SCHEMA_NAME: ""
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending a DELETE request with incorrect operation_id"
      - delete:
          url: "/_/tenant/incorrect_id"
          expect:
            - statusCode: 400
      - think: 1
