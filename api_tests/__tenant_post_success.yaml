# ********RoostGPT********

# Test generated by RoostGPT for test raml-testing using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /_/tenant_post for http method type POST in artillery framework
# RoostTestHash=4b8c14d754
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "__tenant_post_success.csv"
    fields:
      - "module_to"
      - "module_from"
      - "parameters"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Tenant job creation API test"
    flow:
      - log: "Start: Testing Tenant job creation endpoint"
      - post:
          url: "/_/tenant"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            module_to: "{{ module_to }}"
            module_from: "{{ module_from }}"
            parameters: "{{ parameters }}"
          expect:
            - statusCode: 201
            - hasHeader: "Location"
            - hasProperty: "id"
            - hasProperty: "tenant"
            - hasProperty: "tenantAttributes.module_to"
            - hasProperty: "tenantAttributes.module_from"
            - hasProperty: "tenantAttributes.parameters"
            - hasProperty: "complete"
            - hasProperty: "error"
            - hasProperty: "messages"
      - think: 1
      - log: "End: Testing Tenant job creation endpoint"
